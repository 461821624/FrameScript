/**
 * Re-export shared types for backward compatibility
 * All type definitions are now centralized in shared/types.ts
 */
export type {
    VisionResult,
    KeyFrame,
    VideoAsset,
    ScriptSegment,
    Script,
    Project,
    ProjectProgressEvent
} from '../shared/types';

